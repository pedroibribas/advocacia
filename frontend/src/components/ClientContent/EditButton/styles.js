import styled from "styled-components";

export const Container = styled.div`
a {
    display: block;
    height: 4rem;
    padding: 1rem;
    border-radius: 0.25rem;
    background: #7DCE13;
    color: var(--clr-txt);
    font-size: 2rem;
  }
`;